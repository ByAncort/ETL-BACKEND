spring:
  application:
    name: schema-matching-ms
  datasource:
    url: jdbc:mysql://2.25.156.241:3306/etl_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: etl_user
    password: jdsau12341kasd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8085

external:
  api:
    base-url: http://localhost:8082
    integrations:
      path: /api/integrations/connections/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true