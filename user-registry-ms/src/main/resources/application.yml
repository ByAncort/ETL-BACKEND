server:
  port: 9090

spring:
  application:
    name: user-registry-ms
  datasource:
    url: jdbc:mysql://localhost:3306/identity_db
    username: root
    password: 1290
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: diego2005cortes@gmail.com
    password: cgxs xunt rpbg vslw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

identity:
  service:
    url: ${IDENTITY_SERVICE_URL:http://identity-service:9898/api/v1/auth}

app:
  base-url: http://localhost:9090
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true