spring:
  application:
    name: integration-ms
  datasource:
    url: jdbc:mysql://2.25.156.241:3306/etl_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: etl_user
    password: jdsau12341kasd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8082

api:
  register:
    ms:
      url: ${API_REGISTER_MS_URL:http://api-register-ms:8083}
  matcher-ms:
    url: ${SCHEMA_MATCHING_MS_URL:http://host.docker.internal:8000}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
